
Eloquent

コントローラー内で扱うモデルをインポートし、
データを取得する

・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・

use App\Models\Owner; // Eloquent エロクアント

    public function index()
    {
        $e_all = Owner::all();
        dd($e_all);
    }

Owner::all()でOwnerモデルの中身をすべて取得している。